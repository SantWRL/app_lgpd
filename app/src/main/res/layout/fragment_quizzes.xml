<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/transparent"
    android:padding="16dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp"
        android:text="Testes de fixação"
        android:textAppearance="@style/TextAppearance.LGPD.ScreenTitle"
        android:textColor="@color/white"
        android:textSize="24sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:text="Responda por tema para consolidar conceitos e reconhecer decisões corretas em situações reais."
        android:textColor="#E2E8F0"
        android:textAppearance="@style/TextAppearance.LGPD.BodySmall" />

    <!-- Filtro por categoria -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/filterAll"
                style="@style/Button.Secondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:backgroundTint="#F2FFFFFF"
                android:textColor="@color/primary_dark"
                android:text="Todos" />

            <Button
                android:id="@+id/filterFundamentos"
                style="@style/Button.Secondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:backgroundTint="#80FFFFFF"
                android:textColor="@color/white"
                android:text="Fundamentos" />

            <Button
                android:id="@+id/filterDireitos"
                style="@style/Button.Secondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:backgroundTint="#80FFFFFF"
                android:textColor="@color/white"
                android:text="Direitos" />

            <Button
                android:id="@+id/filterSeguranca"
                style="@style/Button.Secondary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:backgroundTint="#80FFFFFF"
                android:textColor="@color/white"
                android:text="Segurança" />

        </LinearLayout>

    </HorizontalScrollView>

    <!-- Lista de quizzes -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/quizzesRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/bg_glass_card"
        android:padding="8dp"
        tools:listitem="@layout/item_quiz_list" />

</LinearLayout>
